{
  "name": "ck3-load-order-manager",
  "version": "0.0.0",
  "scripts": {
    "vite": "vite",
    "tauri-dev": "tauri dev",
    "dev": "npm-run-all -p -r vite tauri-dev",
    "build": "vite build && tauri build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.7",
    "@tauri-apps/cli": "^1.0.0-beta.2",
    "npm-run-all": "^4.1.5",
    "svelte": "^3.37.0",
    "vite": "^2.3.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.0.0-beta.1"
  }
}
